# App template

What `node scripts/incubate.mjs new <slug>` copies. Placeholders: `__SLUG__`, `__NAME__`, `__MOUNT__`, `__PROJECT__`. A Google sign-in, a profile doc with rules and rules tests, one callable, the brand in two files, and the docs every app carries. Grow it from family-hub's patterns.
