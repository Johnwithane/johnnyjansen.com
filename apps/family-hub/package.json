{
  "name": "family-hub",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Family Hub: the family management portal prototype. Self-contained; see ../../docs/INCUBATOR.md.",
  "scripts": {
    "dev": "npm --prefix app run dev",
    "lint": "npm --prefix app run lint",
    "build": "npm --prefix app run build && npm --prefix functions run build",
    "test:run": "npm --prefix app run test:run && npm --prefix functions run test",
    "test:rules": "firebase --project demo-familyhub-rules emulators:exec --only firestore,storage \"vitest run --config vitest.rules.config.ts\"",
    "emulators": "firebase --project demo-familyhub emulators:start",
    "me": "node scripts/me.mjs",
    "deploy:rules": "firebase deploy --only firestore:rules,firestore:indexes,storage",
    "deploy:functions": "firebase deploy --only functions"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.1.0",
    "firebase-tools": "^15.18.0",
    "typescript": "~5.7.2",
    "vitest": "^3.0.5"
  }
}
